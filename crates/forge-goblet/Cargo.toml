[package]
name = "forge-goblet"
version = "0.1.0"
edition = "2021"

[dependencies]
forge-compiler = { path = "../forge-compiler" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
