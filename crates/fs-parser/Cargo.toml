[package]
name = "fs-parser"
version = "0.1.0"
edition = "2021"

[dependencies]
fs-lexer = { path = "../fs-lexer" }
fs-ast = { path = "../fs-ast" }

[dev-dependencies]
test-utils = { path = "../test-utils" }
