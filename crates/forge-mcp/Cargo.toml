[package]
name = "forge-mcp"
version = "0.1.0"
edition = "2021"

[dependencies]
forge-compiler = { path = "../forge-compiler" }
forge-vm = { path = "../forge-vm" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
