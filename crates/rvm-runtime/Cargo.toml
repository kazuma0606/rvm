[package]
name = "rvm-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
rvm-core = { path = "../rvm-core" }
fs-bytecode = { path = "../fs-bytecode" }
rvm-host = { path = "../rvm-host" }

[dev-dependencies]
fs-parser = { path = "../fs-parser" }
fs-compiler = { path = "../fs-compiler" }
